# Poll Email Account Settings
POLL_USERNAME = "john"
POLL_PASSWORD = "smith"
POLL_HOST = "my.pop.server"

DATABASE_ENGINE = 'sqlite3' 

import os
DATABASE_NAME = os.path.join(os.path.dirname(os.path.realpath(__file__)), 'ihp.db')

DEBUG = True
TEMPLATE_DEBUG = True
